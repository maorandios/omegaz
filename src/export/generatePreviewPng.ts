import type { FoldedProfile } from '@/geometry/types'

/** Placeholder — preview PNG optional; returns null if canvas unavailable */
export async function generatePreviewPng(_profile: FoldedProfile): Promise<Blob | null> {
  return null
}
